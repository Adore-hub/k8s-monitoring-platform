groups:
  - name: platform.rules
    rules:
      - alert: PlatformTargetDown
        expr: up == 0
        for: 2m
        labels:
          severity: critical
          level: P0
        annotations:
          summary: "Target is down"
          description: "Target {{ $labels.instance }} is down."
